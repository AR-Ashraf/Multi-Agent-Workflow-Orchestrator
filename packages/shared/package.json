{
  "name": "@cadenza/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cadenza FE/BE contract: the event schema, graph topology, and provider/model registry.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./fixtures": "./src/fixtures/mock-run.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "gen:schema": "tsx scripts/gen-schema.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "zod-to-json-schema": "^3.23.5"
  }
}
